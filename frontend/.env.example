# URL base de la API. Si la dejas vacia, el frontend usa /api y Vite
# lo redirige al backend definido abajo (modo desarrollo).
VITE_API_URL=

# Backend al que apunta el proxy de Vite en desarrollo.
# Con docker compose usa http://localhost:8000
